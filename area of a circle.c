#include<stdio.h>
main()
{
    float radius, area;
    printf("Enter radius of the circle:");
    scanf("%f", &radius);
    area=(22/7)*(radius*radius);
    printf("Area of the circle is: %f",area);
}
