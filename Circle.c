#include<stdio.h>
main()
{
    float radius, area;
    printf("Enter radius of the circle:");
    scanf("%f", &radius);
    area=(3.1416)*(radius*radius);
    printf("Area of the circle is: %f",area);
}

