#include<stdio.h>
main()
{
    float a,b,area;
    printf("Enter a:");
    scanf("%f",&a);
    printf("Enter b:");
    scanf("%f",&b);
    area=(a*b)/2;
    printf("The area of a triangle is %.2f",area);
}

